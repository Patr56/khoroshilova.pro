export enum EStatus {
    IDLE = 'IDLE',
    BEGIN = 'BEGIN',
    SUCCESSES = 'SUCCESSES',
    FAILURE = 'FAILURE',
}

export enum EActions {
    GET_LAST_ALBUMS = 'GET_LAST_ALBUMS',
    GET_ALBUMS = 'GET_ALBUMS',
}